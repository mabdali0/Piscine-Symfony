{% extends 'base.html.twig' %}

{% block title %}Page principale{% endblock %}

{% block content %}
    <h2>Liste des articles</h2>
    <ul>
        {% for slug, content in articles %}
            <li><a href="{{ path('article_page', { article: slug }) }}">{{ slug | capitalize }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}
